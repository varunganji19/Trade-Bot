# Core (the bot runs fully with just these)
numpy>=2.0
pandas>=2.2
# security floors: requests (CVE-2024-35195, CVE-2024-47081), h11 (CVE-2025-43859),
# starlette (TrustedHost host-header bypass CVE-2026-48710 — TrustedHost is the
# dashboard's anti-DNS-rebinding guard, so its bypass matters here)
requests>=2.32.4
h11>=0.16.0
starlette>=0.49.1
ccxt>=4.0
yfinance>=0.2
pyarrow>=15.0        # parquet disk-cache for backtest data
uvicorn>=0.30
fastapi>=0.110
scikit-learn>=1.4     # pulled in by skfolio
skfolio>=1.0          # portfolio allocation + CombinatorialPurgedCV validation

# Dev
pytest>=8.0

# Kronos foundation-model signal is OPTIONAL (the engine degrades to
# "no forecast voter" without it) — install with:
#   pip install -r requirements.txt -r requirements-kronos.txt
# Vendor the model source first: git clone https://github.com/shiyu-coder/Kronos models/kronos
